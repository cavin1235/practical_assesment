import 'package:flutter/material.dart';

/// here we're creating abstract class so that we accidently don't construct this class
abstract class AppColors {
  static const backgroundColor = Colors.amber;
}
